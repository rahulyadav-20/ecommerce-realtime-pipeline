# =============================================================================
# 12-clickhouse.conf â€” ClickHouse log parsing pipeline
# =============================================================================
#
# NOTE: No `input {}` block here. The shared Beats input is in 10-flink.conf.
#
# CLICKHOUSE LOG FORMAT
# â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€
# ClickHouse uses a space-delimited text log format:
#
#   2026.05.11 14:32:10.421001 [ 1234 ] {query-id-or-empty} <Level> Context: message
#
# Field breakdown:
#   Date.Time.microseconds  â†’  timestamp (UTC)
#   [ thread_id ]           â†’  OS thread ID (not a name)
#   {query_id}              â†’  Query UUID or empty {}
#   <Level>                 â†’  Log level: Trace Debug Information Warning Error Fatal
#   Context                 â†’  C++ class / subsystem name (no trailing colon in all lines)
#   message                 â†’  Free-form message
#
# Examples:
#   2026.05.11 14:32:10.421001 [ 12 ] {} <Information> Application: Starting ClickHouse
#   2026.05.11 14:32:10.422001 [ 34 ] {7f1a2b3c-4d5e-6f7a-8b9c-0d1e2f3a4b5c} <Debug> executeQuery: (from 172.20.0.1:59472, using production parser) SELECT 1
#   2026.05.11 14:32:10.500001 [ 56 ] {7f1a2b3c-...} <Warning> MergeTreeDataWriter: Wrote 10000 rows in 0.042 sec., 1000000 bytes. (238095 rows/sec., 23809523.81 b/sec.)
#   2026.05.11 14:32:10.601001 [ 78 ] {7f1a2b3c-...} <Error> DB::Exception: Cannot read from file /var/lib/clickhouse/...
#
# MULTILINE (stack traces)
# â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€
# ClickHouse C++ stack traces follow the pattern:
#   DB::Exception: ... (in main format line)
#   Stack trace:
#   0. ./clickhouse (...)
#   1. ./clickhouse (...)
#
# Filebeat handles multiline merging before shipping (configured in filebeat.yml).
# Logstash receives already-merged events â€” no multiline filter needed here.
# If Filebeat multiline is NOT configured, add a multiline filter:
#
#   multiline {
#     pattern     => "^\d{4}\.\d{2}\.\d{2}"
#     negate      => true
#     what        => "previous"
#     max_lines   => 200
#     timeout     => "5 seconds"
#   }
#
# KEY FIELDS EXTRACTED
# â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€
#   clickhouse.thread_id   â€” OS thread ID
#   clickhouse.query_id    â€” Query UUID (empty for non-query messages)
#   clickhouse.context     â€” Subsystem / class name
#   clickhouse.query_text  â€” SQL text (from executeQuery messages)
#   clickhouse.rows        â€” Rows written/read (from MergeTree messages)
#   clickhouse.elapsed_ms  â€” Query elapsed time in milliseconds
#   clickhouse.database    â€” Database name (from DDL or query context)
#   clickhouse.table       â€” Table name
# =============================================================================

filter {

  if [container][name] =~ /clickhouse/ and [container][name] !~ /clickhouse-ui|ch-ui/ {

    # â”€â”€ Service tag â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€
    mutate {
      add_field => { "[@metadata][service]" => "clickhouse" }
      add_tag   => ["clickhouse"]
    }

    # â”€â”€ Primary grok: full ClickHouse log format â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€
    grok {
      match => {
        "message" => [
          # Full format with query_id
          "(?<log_ts>%{YEAR}\.%{MONTHNUM}\.%{MONTHDAY} %{TIME})\.\d+ \[ (?<clickhouse_thread_id>\d+) \] \{(?<clickhouse_query_id>[^}]*)\} <%{DATA:[log][level]}> (?<clickhouse_context>[^:]+): %{GREEDYDATA:[log][message]}",

          # Format without the query_id braces (some versions omit empty {})
          "(?<log_ts>%{YEAR}\.%{MONTHNUM}\.%{MONTHDAY} %{TIME})\.\d+ \[ (?<clickhouse_thread_id>\d+) \] <%{DATA:[log][level]}> (?<clickhouse_context>[^:]+): %{GREEDYDATA:[log][message]}",

          # Minimal fallback â€” capture at least timestamp + level
          "(?<log_ts>%{YEAR}\.%{MONTHNUM}\.%{MONTHDAY} %{TIME})\.\d+%{SPACE}<%{DATA:[log][level]}>%{SPACE}%{GREEDYDATA:[log][message]}"
        ]
      }
      tag_on_failure => ["_grokparsefailure_clickhouse"]
    }

    if !("_grokparsefailure_clickhouse" in [tags]) {
      date {
        # ClickHouse timestamp: 2026.05.11 14:32:10 (micro-seconds stripped by grok)
        match   => ["log_ts", "yyyy.MM.dd HH:mm:ss"]
        target  => "@timestamp"
        timezone => "UTC"
      }
      mutate { remove_field => ["log_ts"] }
    }

    # â”€â”€ Normalize ClickHouse log levels to ECS lowercase â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€
    # ClickHouse uses: Trace Debug Information Warning Error Fatal
    if [log][level] {
      mutate { lowercase => ["[log][level]"] }
      # "information" â†’ "info"
      if [log][level] == "information" {
        mutate { replace => { "[log][level]" => "info" } }
      }
    }

    # â”€â”€ Extract query SQL text â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€
    # ClickHouse logs the full SQL when a query starts:
    #   executeQuery: (from 172.20.0.1:59472, ...) SELECT count() FROM ecom.events_local
    if [clickhouse][context] =~ /executeQuery|InterpreterSelectQuery/ {
      grok {
        match => {
          "[log][message]" => "\(from\s+%{IP:[client][ip]}:%{POSINT:[client][port]},?[^)]*\)\s+%{GREEDYDATA:[clickhouse][query_text]}"
        }
        tag_on_failure => []
      }
      # Tag long queries (SQL > 500 chars â†’ likely a complex analytical query)
      if [clickhouse][query_text] and [clickhouse][query_text] =~ /.{500}/ {
        mutate { add_tag => ["complex_query"] }
      }
    }

    # â”€â”€ Extract query elapsed time â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€
    # "Read 10000 rows, 1.23 MiB in 0.042 sec., ..."
    # "Processed 10000 rows, 1.23 MiB in 0.042 sec."
    if [log][message] =~ /(?i)in \d+\.\d+ sec/ {
      grok {
        match => {
          "[log][message]" => "in (?<elapsed_sec>\d+\.\d+) sec"
        }
        tag_on_failure => []
      }
      if [elapsed_sec] {
        ruby {
          code => "
            elapsed = event.get('elapsed_sec').to_f
            event.set('[clickhouse][elapsed_ms]', (elapsed * 1000).round)
          "
        }
        mutate { remove_field => ["elapsed_sec"] }
        # Tag slow queries (> 5 seconds)
        if [clickhouse][elapsed_ms] and [clickhouse][elapsed_ms] > 5000 {
          mutate { add_tag => ["slow_query"] }
        }
      }
    }

    # â”€â”€ Extract rows written / read (MergeTree operations) â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€
    # "Wrote 10000 rows in 0.042 sec."
    # "Merged 500000 rows, 48.83 MiB in 2.01 sec."
    if [log][message] =~ /(?i)(?:wrote|merged|read|processed)\s+\d+ rows/ {
      grok {
        match => {
          "[log][message]" => "(?:wrote|Wrote|merged|Merged|read|Read|processed|Processed)\s+(?<clickhouse_rows>\d+)\s+rows"
        }
        tag_on_failure => []
      }
    }

    # â”€â”€ Extract table / database from message â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€
    # "Merging tmp parts in table ecom.events_local"
    # "Creating table ecom.kpi_1min"
    # "Renaming temporary part ... in table ecom.funnel_5min"
    if [log][message] =~ /(?i)table |database / {
      grok {
        match => {
          "[log][message]" => [
            # "table ecom.events_local" â†’ database=ecom, table=events_local
            "table\s+(?<clickhouse_database>[a-zA-Z_][a-zA-Z0-9_]*)\.(?<clickhouse_table>[a-zA-Z_][a-zA-Z0-9_]*)",
            # "database ecom"
            "database\s+(?<clickhouse_database>[a-zA-Z_][a-zA-Z0-9_]*)"
          ]
        }
        break_on_match => false
        tag_on_failure => []
      }
    }

    # â”€â”€ Kafka engine messages â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€
    # "StorageKafka (ecom.events_kafka): Started streaming to 4 consumers"
    if [clickhouse][context] =~ /(?i)kafka/ or [log][message] =~ /(?i)StorageKafka/ {
      mutate { add_tag => ["clickhouse_kafka"] }
      grok {
        match => {
          "[log][message]" => "(?:streaming to|consumers running:)\s+(?<clickhouse_kafka_consumers>\d+)"
        }
        tag_on_failure => []
      }
    }

    # â”€â”€ Materialized view refresh messages â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€
    if [log][message] =~ /(?i)materialized.*view|MV insert/ {
      mutate { add_tag => ["materialized_view"] }
    }

    # â”€â”€ ReplacingMergeTree deduplicate messages â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€
    if [log][message] =~ /(?i)replacing.*duplicate|removing.*duplicate/ {
      mutate { add_tag => ["deduplication"] }
    }

    # â”€â”€ Exception detection â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€
    # ClickHouse exceptions always start with "DB::Exception:"
    if [log][message] =~ /DB::Exception/ or [log][level] == "error" {
      mutate { add_tag => ["application_error"] }
      grok {
        match => {
          "[log][message]" => "DB::Exception:\s+(?<clickhouse_exception_message>[^(]+)"
        }
        tag_on_failure => []
      }
    }

    # â”€â”€ Stack trace detection â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€
    # ClickHouse stack traces look like: "0. ./clickhouse (foo+0x123)"
    if [log][message] =~ /Stack trace:|^\s*\d+\.\s+\.\// {
      mutate { add_tag => ["stack_trace"] }
    }

  } # end if clickhouse

} # end filter

output {

  if "clickhouse" in [tags] {
    elasticsearch {
      hosts => ["http://elasticsearch:9200"]
      index => "logs-clickhouse-%{+YYYY.MM.dd}"
      retry_on_conflict => 3
      action => "index"
    }
  }

}
