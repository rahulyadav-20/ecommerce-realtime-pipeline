# =============================================================================
# E-Commerce Realtime Pipeline — Python Dependencies
# Install: pip install -r requirements.txt
# =============================================================================

# --- Kafka + Schema Registry (Avro) -----------------------------------------
# confluent-kafka[avro] bundles:
#   confluent-kafka       — C-extension Kafka client (librdkafka)
#   confluent-kafka[avro] — AvroSerializer / AvroDeserializer
#   fastavro              — fast Avro encode/decode
confluent-kafka[avro]==2.5.0

# --- Airflow DAG utilities ---------------------------------------------------
# Only needed if running Airflow DAGs locally (not inside the Airflow container)
# apache-airflow==2.9.3

# --- Data quality (Great Expectations DAG) -----------------------------------
# great-expectations==0.18.19

# --- Iceberg / Trino client (optional, for ad-hoc queries) ------------------
# trino==0.329.0

# --- General utilities -------------------------------------------------------
requests==2.32.3          # used by register-schema.sh fallback and health checks
